#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    int a = 5, b = 2;
    cout << "a / b: " << a / b << endl;
    cout << "(float)a / b: " << (float)a / b << endl;
    cout << "(double)a / b:  " << (double)(a / b) << endl;

    return 0;
}